# Openclawssy Environment Configuration
# Copy this file to .env and fill in your values

# REQUIRED: Z.AI API Key for GLM-4.7 Coding Plan
# Get your API key at: https://z.ai/subscribe
# The Coding Plan provides access to GLM-4.7 via OpenAI-compatible endpoint:
# https://api.z.ai/api/coding/paas/v4
ZAI_API_KEY=your-zai-api-key-here

# Optional: Server authentication token (default: change-me)
# This token is required to access the dashboard and API
# IMPORTANT: Use a strong token since the server is accessible over Tailscale
OPENCLAWSSY_TOKEN=change-me

# Optional: Discord bot token for Discord integration
# DISCORD_BOT_TOKEN=your-discord-bot-token-here

# Tailscale Access
# The server binds to 0.0.0.0 by default for Docker/Tailscale compatibility
# Access via: http://<tailscale-ip>:8081/dashboard
# Get your Tailscale IP with: tailscale ip -4
